apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "votingapp_helm.fullname" . }}-test-connection"
  labels:
    {{- include "votingapp_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "votingapp_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
